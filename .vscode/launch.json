{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "node",
            "request": "launch",
            "name": "Launch Program",
            "program": "${workspaceFolder}/index.js", // Path to your Node.js entry file
            "skipFiles": ["<node_internals>/**"]
        }
    ]
}